# SCFA 投稿审核重建方案

更新：2026-09-26。此文件记录已确认的需求和实施顺序，不代表线上已部署。

## 已确认与未知

- 项目所有者确认目标游戏是 Steam 版《Supreme Commander: Forged Alliance》；FAF 专用地图或模组不能仅凭文件结构正常就认定适用。
- 玩家投稿须经过自动检查，之后由管理员决定是否上架。
- 先前使用或计划使用的第三方审核网站名称与实际接入方式未找回，不能当成现存依赖。
- 当前服务已有普通投稿入口和投稿人自己的列表，但管理员审核列表返回 404。旧部署包只有二进制，没有当前服务源码。
- 已取得当前服务二进制并完成离线接口检查，私有备份在 `_源码备份`。仍未取得生产服务器的数据备份与私有配置，也尚未验证 COS 投稿索引的数据格式和迁移方式。
- 当前服务的投稿存储状态为就绪；COS 根目录截图未显示 `submissions/`。完整的 178 条账号审计记录只有一次投稿存储配置事件，没有玩家投稿、审核或发布事件。这说明尚无已记录投稿需要迁移的迹象，但切换前仍要备份并核对真实存储。
- 当前 `E:\SCFA` 游戏目录含 BrewLAN 等附加 `.scd` 文件和多种 MOD，不能直接作为“纯净 Steam 版”的兼容性基线；最终规则和实际载入测试需要一份隔离的干净 Steam 安装。

## 目标流程

1. 玩家登录后投稿。包进入私有待审区域，生成不可变投稿 ID 和 SHA-256。待审内容不进入公开清单。
2. 服务端独立校验 ZIP：文件数、总大小、解压比例、路径穿越、符号链接、重复名称、扩展名及真实格式；解析地图 `_scenario.lua` / `.scmap` 或模组 `mod_info.lua`，核对内部路径、版本和依赖。客户端检查只用于即时反馈，不能代替服务端检查。
3. 对待审包做恶意文件检测。首选评估腾讯云数据万象的 COS 云查毒；其官方文档当前说明可扫描 ZIP，单文件小于 1 GB，需要开通服务。大文件、扫描失败或不可用时保持待审状态，不得自动通过。扫描结果只回答安全风险，不回答游戏兼容性。
4. 在独立的 Steam 版游戏数据基线上做静态兼容性检查：核对引用的游戏资源和 Lua 导入路径，标出只存在于 FAF 的依赖或未知动态引用。结果分为“发现不兼容证据”“需要人工实测”“未发现静态问题”，最后一种也不等于保证可运行。
5. 管理员查看自动报告、包内容及作者说明。对不确定的地图/模组，在隔离的 Steam 游戏环境中实际载入测试，再批准或驳回并写明理由。不得在账号服务器上运行玩家 Lua。
6. 批准后把同一 SHA-256 的包上传到不可变的公开 COS 对象键，核对内容哈希，并在备份和并发检查后更新清单。记录审核人、发布时间、清单版本和回滚点。现有客户端继续按清单版本与内容指纹安全同步。

## 与现有系统衔接

- 保留现有账号服务和公开 COS 清单。新审核服务计划在同域名下提供客户端已经使用的 `/v1/submissions`、`/v1/submissions/mine`、`/v1/admin/submissions` 和审核操作接口。
- 新服务必须使用现有账号服务验证登录会话，并由服务端验证 `review.read`、`review.approve` 权限；不能信任客户端传来的角色。
- 在切换投稿路由之前先备份并检查 `/var/lib/scfa-api` 中现有投稿；确认旧投稿可迁移或明确隔离保留。需要得到当前服务器数据和配置后才能实现并验证这一步。
- 发布器应有独立测试模式，使用测试存储空间和测试清单。正式发布前验证投稿、扫描、驳回、批准、回滚、重复投稿与客户端同步。

## 实施顺序

1. 获取当前服务器二进制、`/var/lib/scfa-api` 的受保护备份及有效服务配置；不要把账号数据、令牌或 COS 密钥上传到公开 GitHub。
2. 用实际 Steam 安装与现有地图/MOD 样本建立兼容性基线和明确的规则测试集。
3. 在 `D:\SCFA内容中心` 新建可测试的审核服务源码与部署说明，先在本机和测试环境验证，再接入待审 COS、病毒检测和公开发布器。
4. 准备生产备份、回滚方案并测试全部流程后再切换服务器路由。每次重大且验证通过的改动同步到 GitHub；私有配置和内容包仅放在受保护备份中。

## 资料来源

- FAF 官方代码对 FAF 模组框架、`mod_info.lua` 和依赖的说明：<https://github.com/FAForever/fa/blob/develop/lua/MODS.LUA>
- FAF 地图版本与实际游戏测试的官方指南：<https://github.com/FAForever/wiki-pages/blob/main/faf-version-guidelines.md>
- 腾讯云数据万象云查毒接口：<https://cloud.tencent.com/document/product/460/63964>
- 腾讯云文件处理费用说明：<https://cloud.tencent.com/document/product/436/82325>
