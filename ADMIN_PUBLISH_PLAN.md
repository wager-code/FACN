# 管理员在软件内发布地图和 MOD

更新：2026-09-26。产品决定取消玩家投稿和投稿审核；只有管理员可以发布。此文件是后续实施计划，当前客户端尚无可用的发布入口。

## 当前做法

管理员通过腾讯云 COS 控制台上传已核对的 ZIP 和缩略图，再更新 `scfa/manifest/latest.json` 或 `scfa/manifest/mods.json`。更新后核对公开清单、下载包 SHA-256、包内游戏版本、首次安装和再次同步跳过结果。不能直接修改玩家游戏目录或把 COS 长期密钥放进客户端。

## 软件内发布的实施顺序

1. 找回或重建账号服务源码，并确认生产服务的数据、路由和回滚方式。保留现有登录、用户和同步接口。
2. 增加仅管理员可用的发布 API。服务端根据登录 Token 和明确的发布权限授权，颁发短期上传凭证或签名 URL；客户端不得持有长期 COS SecretKey。
3. 客户端增加管理员发布页：选择本地地图/MOD，读取游戏名称、内部版本、ID 和预览图；填写发布标签、说明、分类；预览目标清单变更。普通账号不显示也不能调用此入口。
4. 服务端独立验证 ZIP 路径、文件数、大小、压缩比、SHA-256、内部版本、地图/MOD 结构和 Steam 版兼容性。无法自动判断的兼容性由管理员检查，不能标成自动通过。
5. 先把文件上传到暂存位置，校验完成后由服务端原子地发布新清单。保留旧清单和包的回滚信息，记录操作者和发布结果，拒绝重复 ID/版本或覆盖不同内容的同版本包。
6. 在隔离环境验证发布、客户端首次安装、再次同步跳过、版本冲突保护和回滚，再接入生产服务。

## 边界

本次 dev52 只移除玩家投稿及审核客户端代码，不改变服务器已部署路由、COS 对象或历史记录。旧服务端即使仍暴露投稿路由，也不能据此认为“服务器已禁止投稿”；真正禁止需要后续服务端变更并验证。
