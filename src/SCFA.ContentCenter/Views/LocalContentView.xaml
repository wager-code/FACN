<UserControl x:Class="SCFA.ContentCenter.Views.LocalContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

        <Grid Margin="0,0,0,14">
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="{Binding LibraryLabel}" Style="{StaticResource EyebrowText}"/>
                <TextBlock Text="{Binding Title}" Style="{StaticResource PageTitle}" Margin="0,3,0,0"/>
                <TextBlock Text="{Binding Subtitle}" Style="{StaticResource CaptionText}" Margin="0,5,0,0"/>
            </StackPanel>
            <WrapPanel Grid.Column="1" VerticalAlignment="Bottom" Margin="18,0,0,0">
                <Border Style="{StaticResource PillBorder}"><StackPanel Orientation="Horizontal"><TextBlock Text="共 "/><TextBlock Text="{Binding TotalCount}" FontWeight="SemiBold"/></StackPanel></Border>
                <Border Style="{StaticResource PillBorder}"><StackPanel Orientation="Horizontal"><TextBlock Text="有效 "/><TextBlock Text="{Binding ValidCount}" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}"/></StackPanel></Border>
                <Border Style="{StaticResource PillBorder}"><StackPanel Orientation="Horizontal"><TextBlock Text="异常 "/><TextBlock Text="{Binding IssueCount}" FontWeight="SemiBold" Foreground="{StaticResource WarningBrush}"/></StackPanel></Border>
            </WrapPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource ToolbarBorder}" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="340"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <Grid>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Padding="36,8,11,8" ToolTip="按名称、ID、版本、目录或说明筛选"/>
                    <Border Width="17" Height="17" Background="{StaticResource TextMutedBrush}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="12,0,0,0" IsHitTestVisible="False"><Border.OpacityMask><ImageBrush ImageSource="/SCFA内容中心;component/Assets/Fluent/search_regular.png"/></Border.OpacityMask></Border>
                </Grid>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0">
                    <Border Width="7" Height="7" CornerRadius="4" Background="{StaticResource AccentBrush}" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding Status}" Foreground="{StaticResource TextSecondaryBrush}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                </StackPanel>
                <Button Grid.Column="2" Style="{StaticResource SecondaryButton}" Content="重新扫描目录" Command="{Binding RefreshCommand}"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="10" ClipToBounds="True">
            <Grid>
                <DataGrid ItemsSource="{Binding ItemsView}" SelectedItem="{Binding SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" AlternationCount="2" RowHeight="42" ColumnHeaderHeight="38">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="状态" Width="88">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border x:Name="HealthBadge" Background="#43292C" BorderBrush="#734149" BorderThickness="1" CornerRadius="9" Padding="8,3" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <TextBlock x:Name="HealthText" Text="需检查" Foreground="#FFC4C8" FontSize="11"/>
                                    </Border>
                                    <DataTemplate.Triggers><DataTrigger Binding="{Binding Valid}" Value="True"><Setter TargetName="HealthBadge" Property="Background" Value="#173A38"/><Setter TargetName="HealthBadge" Property="BorderBrush" Value="#27645D"/><Setter TargetName="HealthText" Property="Text" Value="正常"/><Setter TargetName="HealthText" Property="Foreground" Value="#A8E9DF"/></DataTrigger></DataTemplate.Triggers>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="2*" MinWidth="190"/>
                        <DataGridTextColumn Header="版本" Binding="{Binding Version}" Width="90"/>
                        <DataGridTextColumn Header="内容 ID" Binding="{Binding Id}" Width="165"/>
                        <DataGridTextColumn Header="目录" Binding="{Binding Folder}" Width="150"/>
                        <DataGridTextColumn Header="文件" Binding="{Binding Files}" Width="68"/>
                        <DataGridTextColumn Header="占用空间" Binding="{Binding SizeText}" Width="92"/>
                        <DataGridTextColumn Header="扫描说明" Binding="{Binding Detail}" Width="2.2*" MinWidth="180"/>
                        <DataGridTemplateColumn Header="操作" Width="96">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource CompactButton}" Content="删除本地" Foreground="#FFDDE0" ToolTip="先备份，再删除此地图或 MOD 的本机文件夹"
                                            Command="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <Border Background="#E607101C">
                    <Border.Style><Style TargetType="Border"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding TotalCount}" Value="0"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></Border.Style>
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"><Border Width="48" Height="48" Background="{StaticResource TextMutedBrush}" Opacity="0.55"><Border.OpacityMask><ImageBrush ImageSource="/SCFA内容中心;component/Assets/Fluent/folder_regular.png"/></Border.OpacityMask></Border><TextBlock Text="当前目录没有检测到内容" Style="{StaticResource SectionTitle}" Margin="0,12,0,0"/><TextBlock Text="请检查软件设置中的玩家内容目录，或重新扫描" Style="{StaticResource CaptionText}" HorizontalAlignment="Center" Margin="0,5,0,0"/></StackPanel>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="3" Style="{StaticResource CardBorder}" Margin="0,12,0,0" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="1.5*"/><ColumnDefinition Width="1.1*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel Margin="0,0,18,0">
                    <TextBlock Text="LOCAL SELECTION" Style="{StaticResource EyebrowText}"/>
                    <TextBlock Text="{Binding SelectedItem.Name, TargetNullValue=请选择本地内容, FallbackValue=请选择本地内容}" FontSize="18" FontWeight="SemiBold" Margin="0,5,0,0" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{Binding SelectedItem.Root, TargetNullValue=选择后可查看实际目录并执行安全操作, FallbackValue=选择后可查看实际目录并执行安全操作}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,6,0,0" TextTrimming="CharacterEllipsis" ToolTip="{Binding SelectedItem.Root}"/>
                    <TextBlock Text="{Binding SelectedItem.Detail}" Foreground="{StaticResource TextMutedBrush}" Margin="0,5,0,0" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
                <Grid Grid.Column="1" Margin="0,1,18,0">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="76"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <Grid.RowDefinitions><RowDefinition/><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
                    <TextBlock Text="结构状态" Style="{StaticResource FieldLabel}"/><TextBlock Grid.Column="1" Text="{Binding SelectedState}" Foreground="{StaticResource AccentBrush}"/>
                    <TextBlock Grid.Row="1" Text="版本 / ID" Style="{StaticResource FieldLabel}"/><TextBlock Grid.Row="1" Grid.Column="1"><Run Text="{Binding SelectedItem.Version, TargetNullValue=—}"/><Run Text="  ·  "/><Run Text="{Binding SelectedItem.Id, TargetNullValue=—}"/></TextBlock>
                    <TextBlock Grid.Row="2" Text="文件 / 大小" Style="{StaticResource FieldLabel}"/><TextBlock Grid.Row="2" Grid.Column="1"><Run Text="{Binding SelectedItem.Files, TargetNullValue=—}"/><Run Text=" 个  ·  "/><Run Text="{Binding SelectedItem.SizeText, TargetNullValue=—}"/></TextBlock>
                </Grid>
                <StackPanel Grid.Column="2" VerticalAlignment="Center" MinWidth="138">
                    <Button Style="{StaticResource SecondaryButton}" Content="打开所在文件夹" Command="{Binding OpenFolderCommand}" Margin="0,0,0,8"/>
                    <Button Style="{StaticResource DangerButton}" Content="删除所选（保留备份）" Command="{Binding UninstallCommand}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
