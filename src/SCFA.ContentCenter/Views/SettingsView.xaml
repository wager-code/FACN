<UserControl x:Class="SCFA.ContentCenter.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="应用设置" Foreground="{StaticResource AccentBrush}" FontSize="12" FontWeight="Bold"/>
                <TextBlock Text="软件设置" FontSize="30" FontWeight="SemiBold" Margin="0,3,0,0"/>
                <TextBlock Text="设置会先保存为草稿，确认无误后才会应用到软件。" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,6,0,0"/>
                <TextBlock Text="{Binding SettingsAudienceHint}" Foreground="{StaticResource TextMutedBrush}" FontSize="11" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top" Margin="16,4,0,0">
                <Border Style="{StaticResource PillBorder}" Margin="0,0,8,0"><TextBlock Text="{Binding DraftStateLabel}" Foreground="{StaticResource AccentBrush}" FontWeight="SemiBold"/></Border>
                <Button Style="{StaticResource CompactButton}" Content="推荐默认值" Command="{Binding RestoreDefaultsCommand}" Margin="0,0,8,0"/>
                <Button Style="{StaticResource SecondaryButton}" Content="测试全部连接" Command="{Binding TestAllCommand}"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,14">
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
            <Border Style="{StaticResource CardBorder}" Padding="13" Margin="0,0,8,0"><StackPanel><TextBlock Text="设置完成度" Style="{StaticResource FieldLabel}"/><TextBlock Text="{Binding ConfigurationScore, StringFormat={}{0}%}" Style="{StaticResource MetricValue}"/><TextBlock Text="已填写项目" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,3,0,0"/></StackPanel></Border>
            <Border Grid.Column="1" Style="{StaticResource CardBorder}" Padding="13" Margin="0,0,8,0"><StackPanel><TextBlock Text="内容目录" Style="{StaticResource FieldLabel}"/><TextBlock Text="{Binding DirectoryHealthLabel}" FontSize="16" FontWeight="SemiBold" Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/><TextBlock Text="Maps / Mods" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,3,0,0"/></StackPanel></Border>
            <Border Grid.Column="2" Style="{StaticResource CardBorder}" Padding="13" Margin="0,0,8,0"><StackPanel><TextBlock Text="登录服务" Style="{StaticResource FieldLabel}"/><TextBlock Text="{Binding ApiHealthLabel}" FontSize="16" FontWeight="SemiBold" Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/><TextBlock Text="安全连接" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,3,0,0"/></StackPanel></Border>
            <Border Grid.Column="3" Style="{StaticResource CardBorder}" Padding="13" Margin="0,0,8,0"><StackPanel><TextBlock Text="云端内容" Style="{StaticResource FieldLabel}"/><TextBlock Text="{Binding CosHealthLabel}" FontSize="16" FontWeight="SemiBold" Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/><TextBlock Text="地图与 MOD" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,3,0,0"/></StackPanel></Border>
            <Border Grid.Column="4" Style="{StaticResource CardBorder}" Padding="13"><StackPanel><TextBlock Text="平台服务" Style="{StaticResource FieldLabel}"/><TextBlock Text="{Binding ServerHealthLabel}" FontSize="16" FontWeight="SemiBold" Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/><TextBlock Text="{Binding ValidationSummary}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,3,0,0"/></StackPanel></Border>
        </Grid>

        <TabControl x:Name="SettingsTabs" Grid.Row="2">
            <TabItem Header="游戏与目录">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,12,0,0">
                    <Grid Margin="0,0,7,0">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="1.5*"/><ColumnDefinition Width="14"/><ColumnDefinition Width="1*"/></Grid.ColumnDefinitions>
                        <Border Style="{StaticResource CardBorder}">
                            <StackPanel>
                                <TextBlock Text="游戏与玩家内容目录" FontSize="18" FontWeight="SemiBold"/>
                                <TextBlock Text="地图和 MOD 必须指向游戏实际读取的已有目录；软件不会创建默认路径。" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,15"/>
                                <TextBlock Text="游戏根目录（可选）" Style="{StaticResource FieldLabel}"/>
                                <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBox Text="{Binding GameRoot, UpdateSourceTrigger=PropertyChanged}"/><Button Grid.Column="1" Style="{StaticResource SecondaryButton}" Content="选择" Command="{Binding BrowseGameCommand}" Margin="8,0,0,0"/></Grid>
                                <TextBlock Text="玩家 Maps 目录" Style="{StaticResource FieldLabel}" Margin="0,12,0,4"/>
                                <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBox Text="{Binding MapsDir, UpdateSourceTrigger=PropertyChanged}"/><Button Grid.Column="1" Style="{StaticResource SecondaryButton}" Content="选择" Command="{Binding BrowseMapsCommand}" Margin="8,0,0,0"/></Grid>
                                <TextBlock Text="玩家 Mods 目录" Style="{StaticResource FieldLabel}" Margin="0,12,0,4"/>
                                <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBox Text="{Binding ModsDir, UpdateSourceTrigger=PropertyChanged}"/><Button Grid.Column="1" Style="{StaticResource SecondaryButton}" Content="选择" Command="{Binding BrowseModsCommand}" Margin="8,0,0,0"/></Grid>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Style="{StaticResource CardBorder}">
                            <StackPanel>
                                <TextBlock Text="目录工具" FontSize="18" FontWeight="SemiBold"/>
                                <TextBlock Text="检测和预检都只修改当前草稿。" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,14"/>
                                <Button Style="{StaticResource SecondaryButton}" Content="自动检测游戏目录" Command="{Binding AutoDetectCommand}"/>
                                <Button Style="{StaticResource SecondaryButton}" Content="使用游戏目录中现有 maps/mods" Command="{Binding UsePlayerDirectoriesCommand}" Margin="0,9,0,0"/>
                                <Button Style="{StaticResource PrimaryButton}" Content="验证已有目录" Command="{Binding ValidateDirectoriesCommand}" Margin="0,9,0,0"/>
                                <Border Style="{StaticResource InsetBorder}" Margin="0,14,0,0" Padding="12"><StackPanel><TextBlock Text="验证结果" Style="{StaticResource FieldLabel}"/><TextBlock Text="{Binding DirectoryStatus}" TextWrapping="Wrap" Margin="0,5,0,0"/></StackPanel></Border>
                            </StackPanel>
                        </Border>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="连接服务（高级）" Visibility="{Binding AdvancedSettingsVisibility}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,12,0,0">
                    <Grid Margin="0,0,7,0">
                        <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="14"/><ColumnDefinition/></Grid.ColumnDefinitions>
                        <Border Style="{StaticResource CardBorder}">
                            <StackPanel>
                                <DockPanel><StackPanel><TextBlock Text="账号 API 与 TLS" FontSize="18" FontWeight="SemiBold"/><TextBlock Text="测试直接使用尚未保存的输入" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,0"/></StackPanel><Button DockPanel.Dock="Right" Style="{StaticResource SecondaryButton}" Content="测试 API" Command="{Binding TestApiCommand}" VerticalAlignment="Top"/></DockPanel>
                                <TextBlock Text="HTTPS 地址" Style="{StaticResource FieldLabel}" Margin="0,16,0,4"/><TextBox Text="{Binding ApiUrl, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Text="安全证书指纹（SHA-256）" Style="{StaticResource FieldLabel}" Margin="0,12,0,4"/><TextBox Text="{Binding ApiPin, UpdateSourceTrigger=PropertyChanged}" FontFamily="Consolas"/>
                                <Border Style="{StaticResource InsetBorder}" Margin="0,14,0,0"><TextBlock Text="{Binding ApiStatus}" TextWrapping="Wrap"/></Border>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Style="{StaticResource CardBorder}">
                            <StackPanel>
                                <DockPanel><StackPanel><TextBlock Text="COS 云端内容" FontSize="18" FontWeight="SemiBold"/><TextBlock Text="只读获取地图与 MOD 正式清单" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,0"/></StackPanel><Button DockPanel.Dock="Right" Style="{StaticResource SecondaryButton}" Content="测试清单" Command="{Binding TestCosCommand}" VerticalAlignment="Top"/></DockPanel>
                                <TextBlock Text="云端存储名称" Style="{StaticResource FieldLabel}" Margin="0,16,0,4"/><TextBox Text="{Binding Bucket, UpdateSourceTrigger=PropertyChanged}"/>
                                <Grid Margin="0,12,0,0"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="12"/><ColumnDefinition/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="服务区域" Style="{StaticResource FieldLabel}"/><TextBox Text="{Binding Region, UpdateSourceTrigger=PropertyChanged}"/></StackPanel><StackPanel Grid.Column="2"><TextBlock Text="内容目录前缀" Style="{StaticResource FieldLabel}"/><TextBox Text="{Binding Root, UpdateSourceTrigger=PropertyChanged}"/></StackPanel></Grid>
                                <Border Style="{StaticResource InsetBorder}" Margin="0,14,0,0"><TextBlock Text="{Binding CosStatus}" TextWrapping="Wrap"/></Border>
                            </StackPanel>
                        </Border>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="启动与更新">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,12,0,0">
                    <Grid Margin="0,0,7,0">
                        <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="14"/><ColumnDefinition/></Grid.ColumnDefinitions>
                        <Border Style="{StaticResource CardBorder}">
                            <StackPanel>
                                <TextBlock Text="软件更新" FontSize="18" FontWeight="SemiBold"/>
                                <TextBlock Text="选择接收稳定版、测试版或开发版更新" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,14"/>
                                <TextBlock Text="更新版本" Style="{StaticResource FieldLabel}"/><ComboBox SelectedValuePath="Tag" SelectedValue="{Binding UpdateChannel}"><ComboBoxItem Content="稳定版（推荐）" Tag="stable"/><ComboBoxItem Content="测试版" Tag="beta"/><ComboBoxItem Content="开发版" Tag="developer"/></ComboBox>
                                <StackPanel Visibility="{Binding AdvancedSettingsVisibility}">
                                    <TextBlock Text="自定义更新地址" Style="{StaticResource FieldLabel}" Margin="0,12,0,4"/><TextBox Text="{Binding UpdateManifestUrl, UpdateSourceTrigger=PropertyChanged}" ToolTip="管理员高级设置；留空时由服务器自动提供更新信息"/>
                                </StackPanel>
                                <CheckBox IsChecked="{Binding AutoCheckUpdates}" Content="启动后自动检查客户端更新" Margin="0,14,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Style="{StaticResource CardBorder}">
                            <StackPanel>
                                <TextBlock Text="启动方式" FontSize="18" FontWeight="SemiBold"/>
                                <TextBlock Text="控制登录页和软件启动时的常用行为" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,16"/>
                                <CheckBox IsChecked="{Binding OfflineAllowed}" Content="登录页允许进入离线模式"/>
                                <Border Style="{StaticResource InsetBorder}" Margin="0,18,0,0"><StackPanel><TextBlock Text="自适应界面" Style="{StaticResource FieldLabel}"/><TextBlock Text="窗口变窄时会自动收起左侧导航，无需手动设置。" TextWrapping="Wrap" Margin="0,5,0,0"/></StackPanel></Border>
                                <Border Style="{StaticResource InsetBorder}" Margin="0,12,0,0"><StackPanel><TextBlock Text="设置保护" Style="{StaticResource FieldLabel}"/><TextBlock Text="未保存的修改不会影响当前下载、登录会话或正在运行的任务。" TextWrapping="Wrap" Margin="0,5,0,0"/></StackPanel></Border>
                            </StackPanel>
                        </Border>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="服务器路径" Visibility="{Binding AdvancedSettingsVisibility}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,12,0,0">
                    <Border Style="{StaticResource CardBorder}" Margin="0,0,7,0">
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="1.45*"/><ColumnDefinition Width="18"/><ColumnDefinition Width="1*"/></Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="服务器功能相对路径" FontSize="18" FontWeight="SemiBold"/>
                                <TextBlock Text="仅接受同一账号服务器下以单个 / 开头的安全相对路径。" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,14"/>
                                <TextBlock Text="普通投稿" Style="{StaticResource FieldLabel}"/><TextBox Text="{Binding SubmissionApiPath, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Text="投稿审核" Style="{StaticResource FieldLabel}" Margin="0,10,0,4"/><TextBox Text="{Binding SubmissionAdminPath, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Text="用户管理" Style="{StaticResource FieldLabel}" Margin="0,10,0,4"/><TextBox Text="{Binding AdminUsersPath, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Text="审计日志" Style="{StaticResource FieldLabel}" Margin="0,10,0,4"/><TextBox Text="{Binding AuditApiPath, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Text="内容历史" Style="{StaticResource FieldLabel}" Margin="0,10,0,4"/><TextBox Text="{Binding ContentHistoryApiPath, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="只读连接检测" FontSize="18" FontWeight="SemiBold"/>
                                <TextBlock Text="不会提交投稿、修改用户或写入审计。" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,14" TextWrapping="Wrap"/>
                                <Button Style="{StaticResource PrimaryButton}" Content="检测服务器功能" Command="{Binding TestServerFeaturesCommand}"/>
                                <Border Style="{StaticResource InsetBorder}" Margin="0,14,0,0" MinHeight="110"><TextBlock Text="{Binding ServerStatus}" TextWrapping="Wrap"/></Border>
                            </StackPanel>
                        </Grid>
                    </Border>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="存储与数据">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,12,0,0">
                    <Border Style="{StaticResource CardBorder}" Margin="0,0,7,0">
                        <StackPanel>
                            <TextBlock Text="软件数据" FontSize="18" FontWeight="SemiBold"/>
                            <TextBlock Text="这里保存软件设置、同步记录、投稿草稿和运行日志，不是游戏地图或 MOD 目录。" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,16" TextWrapping="Wrap"/>
                            <TextBlock Text="软件数据目录" Style="{StaticResource FieldLabel}"/>
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBox Text="{Binding DataDirectory, Mode=OneWay}" IsReadOnly="True"/><Button Grid.Column="1" Style="{StaticResource SecondaryButton}" Content="打开数据目录" Command="{Binding OpenDataFolderCommand}" Margin="8,0,0,0"/></Grid>
                            <StackPanel Visibility="{Binding AdvancedSettingsVisibility}" Margin="0,13,0,0">
                                <TextBlock Text="配置文件（管理员）" Style="{StaticResource FieldLabel}"/>
                                <Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><TextBox Text="{Binding ConfigPath, Mode=OneWay}" IsReadOnly="True"/><Button Grid.Column="1" Style="{StaticResource SecondaryButton}" Content="打开配置目录" Command="{Binding OpenConfigFolderCommand}" Margin="8,0,0,0"/></Grid>
                            </StackPanel>
                            <Border Style="{StaticResource InsetBorder}" Margin="0,16,0,0"><StackPanel><TextBlock Text="内容备份在哪里？" Style="{StaticResource FieldLabel}"/><TextBlock Text="地图和 MOD 的安装备份由软件单独管理，请在左侧“历史回滚”中查看和恢复。" TextWrapping="Wrap" Margin="0,5,0,0"/></StackPanel></Border>
                        </StackPanel>
                    </Border>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <Border Grid.Row="3" Style="{StaticResource ToolbarBorder}" Margin="0,14,0,0">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <Border Style="{StaticResource StatusBadgeBorder}" VerticalAlignment="Center"><TextBlock Text="{Binding BusyStateLabel}" Foreground="{StaticResource AccentBrush}"/></Border>
                <TextBlock Grid.Column="1" Text="{Binding Status}" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="14,0" TextWrapping="Wrap" TextTrimming="CharacterEllipsis"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Style="{StaticResource SecondaryButton}" Content="放弃修改" Command="{Binding DiscardCommand}" Margin="0,0,9,0"/>
                    <Button Style="{StaticResource PrimaryButton}" Content="保存并应用" Command="{Binding SaveCommand}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
