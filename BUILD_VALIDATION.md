# 交付前检查记录

检查日期：2026-08-21

## 已执行

- 68 个 C# 源文件、18 个 XAML 与 `.csproj`：静态枚举和 XML 解析检查通过。
- XAML `x:Class` 与对应 `.xaml.cs` partial class：逐项匹配。
- 空源码文件检查：未发现。
- `TODO` / `FIXME` / `NotImplementedException` 与 dev2-dev5 旧版本标识检查：核心源码无残留。
- 使用本机 .NET SDK 9.0.317 构建 `net8.0-windows` Debug：0 警告、0 错误。
- 执行 `build.ps1 -Configuration Release`：Restore、Release Build、win-x64 Publish 全部成功，0 警告、0 错误。
- 发布文件：`artifacts\win-x64\SCFA内容中心.exe`，FileVersion `4.0.0.11`，ProductVersion `4.0.0-dev11`，SHA-256 `8f21443273a1809c9b3619ced6dfa305581c3d7794349cf88188a8e1d22322c9`。
- 使用隔离的配置、会话和日志目录隐藏启动发布版 4 秒：进程稳定运行，启动冒烟测试通过；测试进程与临时目录均已清理。
- pre-dev6、pre-dev7、pre-dev8、pre-startup-debug、pre-dev9-full 恢复备份均已核验包含 `.sln` 与 `.csproj`，当前备份数量 5，未超过 6 份上限。
- 对照 Go dev51 复核地图/MOD版本来源、正式 manifest 路径、内容 SHA-256 算法、安装与更新目录替换规则。
- 安装器已处理：ZIP hash、content hash、目录穿越、Windows 设备名、重解析点、顶层目录与 `folder_name` 一致性、同卷暂存、独立备份、失败恢复。

## Dev6 本次验证

- 应用版本改为程序集元数据单一来源，UI 与 HTTP 客户端不再硬编码不同 dev 版本。
- 会话文件使用 Windows DPAPI（CurrentUser）保护，并绑定 API 地址和证书指纹。
- 自动登录会校验过期时间、API 身份与 `/v1/auth/me`；401/403 会清除失效会话。
- 首页三个快捷按钮均已绑定真实命令；设置页在落盘前验证 API/TLS/COS 配置。
- 退出登录、离线返回登录页和主窗口切换均已接入。

## Dev7 本次验证

- 本地扫描拒绝磁盘根目录、目录链接和内容链接，并限制发现深度、目录数量与单项文件数量。
- 云端清单限制为 16MB/10000 项；下载包限制为 4GB并检查响应长度和本地磁盘空间。
- ZIP 在写盘前检查大小写重复路径、文件/目录冲突、路径穿越、设备名、非法文件名、总解压大小和异常压缩比。
- 同步遇到模糊本地匹配或内容指纹读取失败时显式记为失败，不再静默跳过或当作缺失安装。
- 同步中心支持取消；安装任务会区分“已取消”和“失败”；手动降级/不可比较版本必须二次确认。
- 反射调用已编译代码完成安全用例：正常 ZIP 解压成功，大小写重复路径和 `../` 路径均被阻止。
- 临时地图扫描得到 1 个有效地图，磁盘根目录扫描在遍历前被阻止；全部测试目录已清理。
- Debug、Release、win-x64 Publish 与隔离启动冒烟测试均通过，0 警告、0 错误。

## Dev8 本次验证

- 云端地图/MOD与本地地图/MOD页面支持按名称、ID、版本、目录和状态即时筛选，并展示所选项目详情。
- 本地内容详情支持直接打开真实内容目录。
- 安装任务提供图形进度、取消和失败/取消后重试；可清理已完成、失败和已取消任务。
- 设置页使用当前未保存参数依次检测账号 API 健康状态及地图/MOD COS 正式清单。
- 任务模型运行时检查通过：运行中任务可取消且回调只触发一次，失败任务可重试。
- Debug、Release、win-x64 Publish 与隔离配置隐藏启动 4 秒均通过，0 警告、0 错误；测试进程和临时数据已清理。

## Dev8 交互调试复核

- 使用 Visual Studio 附加检查确认 WPF UI 线程没有死锁；启动阶段日志进一步断言登录窗口集合数量为 1 且 `IsVisible=True`。
- 修复版本来源在 PowerShell/测试宿主中误读宿主程序集的问题；HTTP 客户端现在始终报告自身版本 `4.0.0-dev8`。
- 使用真实配置完成只读联机验证：TLS SHA-256 证书固定有效，账号 API 健康检查正常（API version 8），地图正式清单 18 项、MOD 正式清单 6 项。
- 当前用户未配置游戏根目录，默认 Maps/Mods 目录不存在，因此本地扫描结果均为 0 项；扫描过程本身无异常。
- 新增 `SCFA_CONTENT_HUB_DATA_DIR`，与配置目录覆盖配合后，调试日志、DPAPI 会话、旧会话迁移路径和配置均可完全隔离。
- 最终发布版在临时数据目录隐藏启动 4 秒，启动日志、登录窗口可见断言及测试数据清理全部通过；Debug/Release/Publish 均为 0 警告、0 错误。

## Dev9 本次验证

- 首次设置支持 Steam 注册表、`libraryfolders.vdf` 多库和 C-Z 常用安装位置检测；目录保存前阻止磁盘根目录、同目录、文件占位和根重解析点。
- 标准玩家 Maps/Mods 目录可一键填入、创建和保存；未准备好目录时主窗口默认进入首次设置页面。
- 诊断中心覆盖版本、系统、配置、应用数据、游戏路径、内容目录、DPAPI 会话、账号 API、投稿/Updater 就绪状态和 COS 清单。
- 使用完全隔离的临时配置/数据/Maps/Mods 运行诊断：9 项检查完成，两个内容目录均判定正常，临时目录全部清理。

## Dev10 本次验证

- 安装覆盖前的备份现在包含 `backup.json`、源版本、来源目录、文件数、大小和 `scfa-content-sha256-v1` 内容指纹。
- 临时 MOD 完成“创建备份 → 篡改当前内容 → 恢复 → 校验旧内容 → 自动生成恢复前安全备份”，恢复结果正确且备份数量为 2。
- 空/越界备份条目恢复被阻止；恢复暂存内容会验证类型结构与 SHA-256，替换失败会原地恢复旧目录。
- Updater 真实只读检查返回：stable 通道服务已就绪、当前尚无发布版本；客户端准确显示无可下载包。
- 自更新端到端测试使用临时旧目标与发布单文件完成真实替换：目标 SHA-256 更新成功、暂存程序和旧程序均清理、新客户端启动且登录窗口可见；所有测试进程与目录已清理。

## Dev11 本次验证

- 投稿打包器仅接受配置内容根目录内的有效项目；拒绝重解析点并限制 100000 文件/4GB。
- 临时 MOD 投稿包包含且仅包含一个顶层目录，`mod_info.lua` 与嵌套文件路径正确，ZIP SHA-256 已计算，测试目录已清理。
- 账号 API 动态路径只接受同一服务器下以单个 `/` 开头的相对路径；绝对外部 URL 测试已被阻止，响应限制为 4MB。
- 投稿、审核、用户/会话、审计、云端历史均不生成演示数据；无权限、404 和服务器错误会在对应页面显式显示。
- 任务历史原子保存到隔离 `tasks.json`；重载后运行中任务正确变为“已中断”，进度保留，清理结果也能持久化。
- 静态检查：68 个 C#、18 个 XAML，XML 全部可解析，所有 `x:Class` 均有对应 code-behind；无空源码、TODO/FIXME/NotImplementedException、合并冲突或 dev2-dev9 旧版本标识。
- Debug、Release、win-x64 Publish 均成功，0 警告、0 错误。

## Dev12 稳定性加固验证

- 显示名称相同但 ID/目录不同的内容不再进入自动覆盖；规范化 ID 和云端目录别名仍能正确匹配。
- `alpha1` 与 `beta1` 不再误判为同版本；`v1.0.0` 与 `1`、`1.9` 与 `1.10` 的兼容和排序保持正确。
- 投稿上传不再受普通 JSON API 的 12 秒全局超时限制；任务取消仍可立即终止上传。
- 安装下载使用清单 `size` 作为上限，并在解压前核对实际 ZIP 长度；投稿 ZIP 完成后再次检查最终包大小。
- 损坏配置会在默认设置接管前创建 `.invalid_*` 备份并向登录窗口提示；并发保存测试生成有效 JSON。
- 登录成功后立即调用 `/v1/auth/me` 复核服务器身份和权限。
- 自更新执行前重新计算暂存 EXE 的 SHA-256；更新器启动失败时恢复旧客户端。
- 13 项核心回归测试全部通过；解决方案 Debug/Release 构建均为 0 警告、0 错误。
- win-x64 单文件发布成功；成品 `ProductVersion=4.0.0-dev12`、`FileVersion=4.0.0.12`、大小 626889 字节、SHA-256 `26A92000DA03ED0B97AEEBDB3D7C210364657D87C19C2E9369677B0146B339FE`；隔离目录隐藏启动 4 秒未提前退出。

## Dev13 同步主链第一批验证

- COS 内容下载与清单请求分离超时策略：清单保持 20 秒上限，大文件下载不再被全局 20 秒中止；连接 30 秒无响应或传输连续 45 秒无数据才触发瞬态重试。
- 模拟首次传输在 96KB 处断开，第二次请求正确携带强 ETag 与 Range，从断点完成续传，最终字节逐一一致；最多自动尝试 3 次，用户取消不被重试吞掉。
- 无效本地内容仍参与高置信度 ID/目录匹配，决策测试确认进入“本地内容损坏”备份后修复流程；本地较新版本继续阻止自动降级。
- 游戏根目录必须包含可执行文件才会被自动识别；未配置 Maps/Mods 时始终使用玩家文档目录，不会把官方游戏目录中的原生内容当作安装目标。
- 设置和首次向导统一校验内容目录：必须为绝对路径，拒绝磁盘根、重解析点、同路径和互相嵌套路径；旧无效配置会回到设置向导而不是导致启动崩溃。
- 本地内容页增加带二次确认的安全卸载；真实临时目录完成“完整备份 → 从玩家目录移除 → 读取备份并核对原文件”，云端内容页同时识别损坏项并提供修复安装。
- 21 项核心回归测试全部通过；解决方案 Debug/Release 构建均为 0 警告、0 错误。
- win-x64 单文件发布成功；成品 `ProductVersion=4.0.0-dev13`、`FileVersion=4.0.0.13`、大小 638665 字节、SHA-256 `A22EF7C6E664F0F4B207FA302440F81FA1455D601208D31E487A6A46172A286D`；D 盘项目内隔离目录隐藏启动 4 秒未提前退出，临时目录已清理。

## Dev14 内容中心验证

- 只读核对生产 COS 清单：地图 18 项、MOD 6 项；当前正式条目提供 aliases/content_sha256/file/folder_name/id/name/published_at/sha256/size/version，客户端同时向前兼容 author/description/category/tags 可选字段。
- 云端地图/MOD页面支持全文搜索、安装状态/分类/收藏/最近安装筛选，以及名称、发布时间、大小和安装状态排序；无扩展元数据时显示明确缺省文字。
- 勾选状态通过属性通知即时同步到界面；可勾选当前筛选结果、清除勾选、批量安装和取消。批量流程逐项隔离失败，只处理缺失、旧版、损坏或同版本指纹不一致内容，不自动降级本地较新版或不可比较版本。
- 收藏键按“类型:内容ID”去重持久化；成功安装自动置顶最近内容记录并最多保留 50 项。
- 内存构造的完整地图 ZIP 通过真实下载、长度检查、安全解压和安装流程进入隔离玩家 Maps 目录，并正确写入最近安装记录；随后安全卸载与备份测试仍通过。
- 30 项核心回归测试全部通过；Debug/Release 构建均为 0 警告、0 错误。
- win-x64 单文件发布成功；成品 `ProductVersion=4.0.0-dev14`、`FileVersion=4.0.0.14`、大小 656585 字节、SHA-256 `5BE37290A80D6496D218959964405F309F948BCC47F5B5329DE99E42F2792475`；D 盘项目内隔离目录隐藏启动 4 秒未提前退出，临时目录已清理。

## Dev15 玩家投稿主链验证

- 投稿中心现在支持选择或拖入玩家 Maps/Mods 中的单个内容目录，只接收当前配置内容根下并通过地图/MOD结构校验的项目。
- 投稿表单增加名称、文件内版本、作者、说明、分类和标签；版本必须与实际内容一致，说明限制 10—2000 字，标签去重且最多 10 个。
- 可选预览图限制为真实 PNG/JPEG、5MB、320×180 至 4096×4096；扩展名与文件签名不一致会在保存草稿或上传前阻止。
- 每个内容的草稿按稳定内容键原子保存，最多保留 100 份、总文件上限 1MB；损坏草稿文件会隔离而不是覆盖，投稿成功后才自动清理，取消或失败时保留。
- 投稿 ZIP 继续执行单顶层安全打包、重解析点/数量/大小限制与内容/包双 SHA-256；上传流按真实发送字节更新 0—100% 进度，取消与失败任务可以从保存的草稿重试。
- 对生产账号服务仅执行只读探测：`/healthz` 返回 version 8、`submission_ready=true`；`GET /v1/submissions/mine` 返回需要身份认证，确认路由存在，未发送 POST、未创建生产投稿。
- 40 项核心回归测试在 Debug 与最终 Release 代码上全部通过，其中新增 10 项覆盖投稿标签、版本/说明边界、上传字节和进度、预览图格式/尺寸、草稿保存与清除；Debug/Release 构建均为 0 警告、0 错误。
- win-x64 单文件发布成功；成品 `ProductVersion=4.0.0-dev15`、`FileVersion=4.0.0.15`、大小 693961 字节、SHA-256 `DCBADADD3CEC75616DE6F72F5EBF2CAFF70057A9C957A972E42202F52D9EE0F9`；使用项目内隔离配置/数据目录隐藏启动 5 秒未提前退出，测试进程与临时目录均已清理。

## Dev16 软件设置验证

- 设置页不再直接绑定并修改 `ConfigService.Current`；进入页面时建立完整深拷贝，编辑、恢复默认值和连接测试均作用于草稿，只有全量校验与原子文件替换成功后才切换运行配置。
- “放弃修改”重新读取已保存快照；“载入推荐默认值”只重置公开的软件参数，保留登录账号记录、收藏、最近内容和未来版本未知字段。
- 目录支持自动检测游戏、标准玩家目录、只验证不创建；保存时才创建 Maps/Mods。游戏根目录必须包含已知 SCFA 可执行文件，内容目录继续拒绝磁盘根、重解析点、相同或互相嵌套路径。
- 账号 API、COS 地图/MOD正式清单、投稿/更新健康状态提供独立和整体只读检测，并直接使用尚未保存的输入；服务器功能路径拒绝绝对 URL、跨主机、查询/片段、空白、反斜杠和 `..` 路径。
- 配置服务新增候选快照保存接口：深拷贝保留 `JsonExtensionData`，临时文件成功替换后才更新 `Current`；API/TLS 改变时清除绑定旧服务器的 Token/会话并返回登录页。
- 52 项核心回归测试覆盖设置草稿隔离、候选配置切换、未知字段保留、目录延迟创建与全部新增校验边界；设置页隔离编译和完整 Release 构建均为 0 警告、0 错误。
- win-x64 单文件发布成功；成品 `ProductVersion=4.0.0-dev16`、`FileVersion=4.0.0.16`、大小 718537 字节、SHA-256 `9500034ED08A64014FE9AE8698DE123AA63A2C263874EC344687F8FB7B363A4A`；发布版使用项目内隔离配置/数据目录隐藏启动 5 秒未提前退出，测试进程与临时目录均已清理。

## Dev17 UI 视觉系统验证

- 全局颜色、文字层级、边框、圆角和阴影统一为克制的深蓝黑科幻风；主操作使用冷蓝渐变，成功状态使用青绿色，警告和危险色仅用于真实状态。
- 重做 TextBox、PasswordBox、ComboBox（含可编辑模式）、CheckBox、Button、DataGrid、TabItem、ProgressBar、ToolTip 和 ScrollBar 模板；焦点、悬停、按下、禁用、选中与拖动状态均有明确反馈。
- 主窗口使用 Windows `WindowChrome` 自绘框架，导航改为带 Fluent 图标和左侧状态轨的单选导航；顶栏显示当前页面、说明和在线/离线工作区，窗口较窄时侧栏折叠为 82px 图标模式。
- 登录页改为内容品牌区与账号表单双栏结构，展示实际安全能力；空提示区域自动隐藏，登录、注册、自动会话恢复和离线入口原有逻辑保持不变。
- 首页改为真实内容指挥台：显示有效地图、有效 MOD、任务记录、运行任务和结构问题，提供扫描、同步及只读服务检查，并展示最近操作状态和安全工作流。
- 使用隔离配置实际启动并截图复核：1040×660 登录窗口、1586×992 首页、1586×992 首次设置页和最小 980×680 紧凑首页均未出现横向溢出；视觉复核发现并修复空警告框、黑色显式标题、白色系统下拉框、折行头像和白色系统滚动条。
- 56 项核心回归测试全部通过，新增检查全部 XAML 可解析、设计系统关键资源存在、自绘主框架/导航存在及主导航图标文件完整；完整 Release 构建为 0 警告、0 错误。
- win-x64 单文件发布成功；成品 `ProductVersion=4.0.0-dev17`、`FileVersion=4.0.0.17`、大小 752841 字节、SHA-256 `766BEB2ED496B8D5B0E73D9FF899D6161C4B2656C8A019141D7E05B03DB95237`；发布版使用项目内隔离配置/数据目录隐藏启动 5 秒未提前退出，测试进程与临时目录均已清理。

## Dev17.1 运行时绑定热修复验证

- 用户运行日志定位到投稿页 `DraftVersion` 只读属性被 WPF 默认 `TwoWay` 绑定回写，触发 `InvalidOperationException`；投稿版本显式改为 `Mode=OneWay`，设置页的配置文件与应用数据只读路径同步加固。
- 新增全量 XAML 只读文本框绑定检查；57 项核心回归测试全部通过，并在独立 STA/WPF 进程中真实加载投稿页面、完成布局绑定，未再产生只读属性回写异常。
- 完整 Release 构建为 0 警告、0 错误；win-x64 单文件成品 `ProductVersion=4.0.0-dev17.1`、`FileVersion=4.0.0.17`、大小 753865 字节、SHA-256 `7B12E7CB729B5B00A47C6B2DA710232E9FB44BDF668581A1539633DE16EA69D8`。

## Dev18 内容管理 UI 第二阶段验证

- 云端地图/MOD页面增加目录总数、已安装、待处理统计，统一搜索/筛选工具栏、收藏与安装状态徽标、批量操作及所选内容详情；数据来自正式清单和本地扫描，不生成演示条目。
- 本地地图/MOD页面增加总数、有效、异常统计，使用结构状态徽标、目录搜索、空状态和所选内容安全操作区；卸载仍要求二次确认并先建立完整备份。
- 同步中心重构为同步范围、安全原则、当前进度、最近状态和逐项执行记录控制台；下载任务增加全部/运行/完成/失败统计、进度、状态徽标、取消与重试入口。
- 使用隔离配置真实启动并逐页鼠标点击检查云端地图、本地地图、同步中心和下载任务；1500×900 四页标题与内容正确切换，运行日志无未处理 UI 异常。980×680 下云端筛选自然换行、表格与详情可用，同步双栏及执行记录无横向溢出。
- 59 项核心回归测试全部通过，新增内容页通用样式及四页真实状态绑定检查；投稿页面独立 STA/WPF 运行测试继续通过。Debug 与 Release 均为 0 警告、0 错误。
- win-x64 单文件发布成功；成品 `ProductVersion=4.0.0-dev18`、`FileVersion=4.0.0.18`、大小 771273 字节、SHA-256 `69F09A4224D9B3CF00AB508476F3BC33A57612A3DDF079F3BFCEB81BEA0CB3C8`。

## Dev19 投稿与维护工作区 UI 第三阶段验证

- 投稿中心重构为四步投稿工作流，增加本地内容/投稿记录/草稿完成度、说明字数和标签数量等真实统计；保留拖放、草稿、预览图、安全校验、取消和投稿记录详情主链。
- 本地备份增加总数、地图/MOD、容量与完整性统计；云端历史增加正式内容/历史版本统计和版本选择详情；诊断中心增加正常/需处理/检查范围健康卡；客户端更新增加通道、安全校验、当前/最新版本与暂存状态控制台。
- 真实 WPF 预览发现并修复投稿页 `Run.Text` 对只读统计属性的默认回写问题，三个统计绑定显式设为 `Mode=OneWay`；独立投稿页面绑定测试覆盖扩展后的只读探针并通过。
- 使用隔离配置真实启动发布版，逐页鼠标点击检查历史回滚、云端历史、诊断中心和客户端更新；投稿页使用相同应用资源与测试数据隔离渲染。1500×900 和 980×680 共 10 张检查图均复核通过，紧凑导航、表格、状态卡和操作区无横向溢出；运行日志未发现未处理 UI 异常。
- 修复 Windows PowerShell 5.1 对无 BOM 中文构建脚本的解析问题，并让回归脚本在 `dotnet` 未加入 PATH 时自动使用 Visual Studio 常见 SDK 路径；两个脚本均在本机直接执行通过。
- 61 项核心回归测试全部通过；投稿页面独立 STA/WPF 运行测试通过；最终 Debug、Release 和 win-x64 Publish 均为 0 警告、0 错误。
- win-x64 单文件发布成功；成品 `ProductVersion=4.0.0-dev19`、`FileVersion=4.0.0.19`、大小 788169 字节、SHA-256 `062D61F5A9B4E088EEFE4B1156572B994909DFCE884F3EDE17A08160EEE1F5D8`。

## Dev20 管理员工作区 UI 第四阶段验证

- 投稿审核改为真实队列与决策台：显示待审核总数、地图/MOD数量、队列容量，所选投稿展示作者、分类、标签、文件数、包大小、内容说明、包/内容双 SHA-256；通过与拒绝继续使用原权限校验和二次确认。
- 用户管理增加总用户、正常/受限账号、活动会话统计和即时搜索；所选用户操作台集中显示账号标识、权限数量、角色、状态、会话和登录信息，保存与撤销会话继续受原 RBAC 权限保护。
- 服务器审计增加账号 API、投稿、Updater、直连四项真实健康卡，支持按操作者、操作、目标、结果、详情和 IP 搜索服务器审计记录，显示可见数、异常数和所选记录详情；客户端不生成或补写审计数据。
- 全局 DataGrid 选中单元格不再使用系统白色高亮，统一为深蓝行选中态；用户操作台和审计详情在 980×680 下支持垂直滚动，角色、状态和详细内容始终可达。
- 使用真实 WPF 资源和隔离测试数据渲染投稿审核、用户管理、服务器审计三页，1500×900 与 980×680 共 6 张视觉检查图逐张复核通过；三个页面独立 STA/WPF 加载与布局绑定测试通过。
- 62 项核心回归测试全部通过；投稿页面运行时绑定测试继续通过；最终 Debug、Release 和 win-x64 Publish 均为 0 警告、0 错误。
- win-x64 单文件发布成功；成品 `ProductVersion=4.0.0-dev20`、`FileVersion=4.0.0.20`、大小 801993 字节、SHA-256 `06BE5E4E2E5422A3C49DC5FFFFD8BF257C2623993D5A6B6AC34ABA4633AE4489`。

## Dev21 设置工作区 UI 第五阶段验证

- 首次设置改为三步安全工作区，实时显示游戏目录检测数、设置完成度、游戏目录/玩家目录状态和最终可保存状态；创建目录仍只发生在最终确认保存时。
- 软件设置改为游戏与目录、网络与内容、客户端行为、服务器路径、本机数据五类标签页，顶部使用真实配置完整度及目录/API/COS/服务器健康摘要，底部保存操作区在滚动时保持可见。
- 设置输入继续写入独立草稿；推荐默认值、目录预检和全部连接检测不会提前污染当前运行配置，只有全量校验与原子保存成功后才切换服务配置。
- 使用真实 WPF 资源和隔离探针渲染首次设置、软件设置两页，1500×900 与 980×680 共 4 张视觉检查图逐张复核通过；紧凑尺寸下正文可滚动，标题、健康卡、标签页和固定保存区无重叠。
- 63 项核心回归测试全部通过；投稿、三个管理员页面、首次设置与软件设置共三组独立 STA/WPF 运行时绑定测试全部通过；最终 Debug、Release 和 win-x64 Publish 均为 0 警告、0 错误。
- win-x64 单文件发布成功；成品 `ProductVersion=4.0.0-dev21`、`FileVersion=4.0.0.21`、大小 810697 字节、SHA-256 `5C8E8CF49DD23D8BA3709B013CEAC7D44F04710471D1E0D0161A55CAA3D70F8F`。

## Dev21.1 非标准游戏主程序兼容修复验证

- 游戏根目录识别不再只依赖固定 EXE 名称：同时检查根目录及 `bin`，标准名称保持兼容；改名程序要求有效 PE 头、同目录 `game.dat` 和至少两项 SCFA 特征文件，避免把普通软件目录误识别为游戏。
- 常见改名 `game.exe` 优先于 `BsSndRpt.exe` 等辅助程序；其他未知名称仅从不小于 1MB 的 PE 中按体积筛选。设置页目录验证会显示实际识别到的相对主程序路径；游戏根目录仍可留空，地图/MOD 同步只依赖独立玩家目录。
- 使用本机真实目录验证：`E:\SCFA` 识别成功，最终准确定位 `E:\SCFA\bin\game.exe`。未运行或修改游戏主程序，也未向游戏安装目录写入内容。
- 65 项核心回归测试全部通过，新增覆盖带崩溃报告器干扰的 `bin\game.exe` 安装结构；首次设置与软件设置独立 STA/WPF 绑定测试通过；最终 Debug、Release 和 win-x64 Publish 均为 0 警告、0 错误。
- win-x64 单文件发布成功；成品 `ProductVersion=4.0.0-dev21.1`、`FileVersion=4.0.0.21`、大小 812233 字节、SHA-256 `BAF587C45EFD18FDF7CED6084DC66BEBFA12B77E857D02832765A340B5631E59`。

## V4.0.0-dev22（2026-08-22）

- 修改前备份：`D:\SCFA内容中心_源码备份\V4.0.0-dev21.1_20260822_124730`，742 个文件；备份总数保持 6 份并轮换最旧项。
- Release 解决方案构建和项目发布均成功，0 警告、0 错误；75 项核心回归测试全部通过。
- 新增严格一致性实测：同版本地图目录加入额外文件后完整目录指纹必然变化；修复采用备份后整目录替换，活动目录多余文件被移除，备份中仍可恢复。
- 目录策略实测：不存在的 Maps/Mods 被拒绝且不会创建；只接受用户选择或游戏目录中已经存在的内容根目录，不再回退到 C 盘“我的文档”。
- UI/XAML 检查通过：方案 A 状态导航、战略背景、云历史搜索、内容预览/无图占位、版本/大小/标签/安装路径绑定均存在且引用资源有效；1280×800 设置页视觉渲染成功。
- 关闭流程修复：用户关闭登录窗口或主窗口会显式结束应用；只有登录页与主界面的内部交接会抑制退出，避免无窗口后台残留进程。
- win-x64 单文件发布成功；成品 `ProductVersion=4.0.0-dev22`、`FileVersion=4.0.0.22`、大小 824009 字节、SHA-256 `A4938487A772F17369352EC70E8F9F3B0A9C47DC9DE836BE51C4081CFAE16814`。

## V4.0.0-dev22.1（2026-08-22）

- 修改前完整备份：`D:\SCFA内容中心_源码备份\V4.0.0-dev22_20260822_133000`，按包含隐藏文件口径核对为 786/786；备份总数保持 6 份。
- 修复战略网格背景的 WPF `GeometryDrawing.Pen` 运行时类型转换异常：改用显式 `Pen` 对象，避免登录成功后主窗口构造失败。
- TextBox 与 PasswordBox 内容宿主绑定 `VerticalContentAlignment`，账号、邮箱和密码文本在 42px 输入框中垂直居中，不再贴底或被裁切。
- 新增主程序真实入口参数 `--main-window-construction-smoke`；Release apphost 与最终单文件 EXE 均完成隐藏主窗口构造/布局并以退出码 0 结束。
- Release 构建 0 警告、0 错误，75 项核心回归测试全部通过；1280×800 输入框视觉渲染复查通过。
- 最终单文件成品 `ProductVersion=4.0.0-dev22.1`、`FileVersion=4.0.0.22`、大小 823497 字节、SHA-256 `BA4E2115210CF5A8F8DB3AAAA365E60620E314C68654A781B7F9A322C4784D10`。

## V4.0.0-dev22.2（2026-08-22）

- 沿用修改前完整备份：`D:\SCFA内容中心_源码备份\V4.0.0-dev22_20260822_133000`，包含隐藏文件核对为 786/786；备份总数保持 6 份。
- 登录输入框模板改为边框承载内边距、内容宿主垂直居中；1040×660 真实 WPF 窗口渲染验收通过，账号文字和密码圆点完整显示，截图为 `artifacts\ui-dev22.2\login-1040x660.png`。
- 增加记住账号与记住密码。密码只在登录成功后使用 Windows DPAPI `CurrentUser` 范围加密保存，损坏密文会被拒绝并清除，不保存明文；取消对应选项会清除缓存。
- 登录与注册输入项建立明确 Tab 顺序；账号/邮箱按 Enter 前进到下一输入框，登录页通过默认按钮支持 Enter 直接登录。
- 最终 Release 构建 0 警告、0 错误，80 项核心回归测试全部通过；源程序和最终单文件的隐藏主窗口构造测试均通过，最终测试退出码为 0，进程残留数为 0。
- 最终单文件成品 `ProductVersion=4.0.0-dev22.2`、`FileVersion=4.0.0.22`、大小 828105 字节、SHA-256 `2AF36367FA9A426BA49205E0426EE85531D49F6DB7B8DD83C135C3BF19D71F65`。

## V4.0.0-dev23（2026-08-22）

- 修改前完整备份：`D:\SCFA内容中心_源码备份\V4.0.0-dev22.2_20260822_150412`；创建后删除最旧 dev18 备份，备份总数保持 6 份。
- 总览与同步中心复用同一 `SyncPageViewModel`，从总览启动后切换页面仍显示进度、结果并可取消；同步服务增加非阻塞互斥，安装与卸载增加统一操作互斥，避免并发替换同一内容目录。
- 地图扫描同时接受 `map_version` 与旧版/标准地图的 `version` 字段；新增真实目录分析回归用例，避免原版地图被批量误报异常。
- 全部展示型 DataGrid 默认只读并裁剪越界内容；云端批量勾选改用模板复选框。用户与审计列表在 900×640 下改为紧凑双行结构，视觉复核无文字重叠。
- Release 构建 0 警告、0 错误，84 项核心回归测试全部通过；投稿、管理员、首次设置与软件设置真实 WPF 绑定测试通过。
- 最终单文件隐藏主窗口构造测试退出码 0，进程残留数 0；成品 `ProductVersion=4.0.0-dev23`、`FileVersion=4.0.0.23`、大小 830153 字节、SHA-256 `E05A04CEBE1C08AF973779B180187E9A1DCFB01E437978A5AB2FF55CD0AA4137`。

## V4.0.0-dev24（2026-08-22）

- 修改前完整备份：`D:\SCFA内容中心_源码备份\V4.0.0-dev23_20260822_153731`；备份总数保持 6 份。
- 登录页默认勾选“记住账号”；增加可输入、可下拉切换的多账号记录，最多保留 8 个账号；旧版单账号配置自动迁移。
- 增加“自动登录”选项，默认关闭；只有用户明确勾选后才保存并恢复登录会话。记住密码和自动登录会自动要求记住账号，取消记住密码会关闭自动登录。
- 密码仍使用 Windows 当前用户 DPAPI 加密保存；客户可见文案移除 COS、TLS、证书指纹等实现术语，改为同步、安全连接和隐私说明。
- Release 构建 0 警告、0 错误，85 项核心回归测试全部通过；登录页 1040×660 真实 WPF 渲染通过，截图为 `artifacts\ui-dev24\login-1040x660.png`。
- 最终单文件隐藏主窗口构造测试退出码 0，进程残留数 0；成品 `ProductVersion=4.0.0-dev24`、`FileVersion=4.0.0.24`、大小 836809 字节、SHA-256 `0CA6795FD9AAB62A5EB06F0A80D2821DB6A4FF3DEB57278F9815FDDA2859A2CF`。

## V4.0.0-dev25（2026-08-22）

- 修改前完整备份：`D:\SCFA内容中心_源码备份\V4.0.0-dev24_20260822_161029`；备份总数保持 6 份，备份文件 864 个。
- 普通客户默认隐藏连接服务和服务器路径；设置页和诊断页的客户文案已调整，设置完成度按当前可见字段计算。
- 主界面战略网格、轨道线和等高线背景增强，降低深色遮罩对背景的覆盖；未添加第三方图片资源。
- Release 解决方案编译通过，0 警告、0 错误；核心回归测试全部通过。
- 本次环境未能生成新的 win-x64 单文件：运行时资产还原访问 NuGet 时 TLS 凭证失败；已生成可运行的框架依赖测试包 `artifacts\win-x64-dev25`，隐藏主窗口构造退出码 0。正式单文件发布包需在可正常还原 .NET 运行时资产的环境重新生成。

## V4.0.0-dev26（2026-08-22）

- 修改前完整备份：`D:\SCFA内容中心_源码备份\V4.0.0.25_20260822_163832`；备份总数保持 6 份。
- 同步中心增加 `sync-history.json` 本地历史，完成、取消和失败均保存，最近 30 次记录通过临时文件替换原子写入；重新打开软件可恢复最近一次同步结果。
- 同步中心增加本次记录/历史记录计数，并明确提示记录会跨页面和重启保留。
- Release 解决方案编译通过，0 警告、0 错误；核心回归测试全部通过，新增同步历史跨实例读写验证通过；隐藏主窗口启动冒烟测试退出码为 0。正式单文件发布仍受当前环境 .NET 运行时包还原条件限制。

## V4.0.0-dev27（2026-08-22）

- 修改前完整备份：`D:\SCFA内容中心_源码备份\V4.0.0.26_ui-background_20260822_165412`；备份总数保持 6 份。
- 主窗口背景增加斜向光带、连续等高线、分段航迹和多层环形结构；降低遮罩强度，避免背景过暗或内容区域被纹理干扰。
- Release 编译、核心回归测试和隐藏主窗口启动冒烟测试均通过；真实账号、生产内容和单文件发布仍按用户安排暂不验收。

## V4.0.0-dev28（2026-08-22）

- 修改前完整备份：`D:\SCFA内容中心_源码备份\V4.0.0.27_image-background_20260822_171533`；备份总数保持 6 份。
- 接入用户确认的 `2048×1152` 指挥舰桥背景图，主内容区使用 `UniformToFill` 自适应铺满，图片下方叠加深色可读性遮罩，原有战术纹理降为低透明度。
- Release 编译、核心回归测试和隐藏主窗口启动冒烟测试均通过；真实账号、生产内容和单文件发布仍按用户安排暂不验收。

## V4.0.0-dev29（2026-08-22）

- 修改前完整备份：`D:\SCFA内容中心_源码备份\V4.0.0.28_background2_20260822_172945`；备份总数保持 6 份。
- 替换 `Assets\command_deck_background.png` 为用户确认的 `1586×992` 蓝色行星与机甲背景，图片使用 `UniformToFill`，透明度 0.52，前景遮罩 0x82。
- Release 编译、核心回归测试和隐藏主窗口启动冒烟测试均通过；真实账号、生产内容和单文件发布仍按用户安排暂不验收。

## V4.0.0-dev30（2026-08-22）

- 修改前完整备份：`D:\SCFA内容中心_源码备份\V4.0.0.29_cloudhistory-binding_20260822_174502`；备份总数保持 6 份。
- 修复云端历史页面进入时的 WPF 绑定错误：只读统计属性改为明确单向绑定。
- Release 构建、核心回归测试和指定发布目录输出均通过；`artifacts\win-x64` 当前为框架依赖发布（EXE、DLL、runtimeconfig 和 deps 文件），因为本机资产文件尚未包含 `win-x64` 目标，单文件发布暂未生成；真实账号和生产内容仍按用户安排暂不验收。

## 仍需有授权账号/真实内容的验收

- 使用真实账号完成登录、重启自动登录、退出登录和 Token 过期流程。
- 使用生产 API/COS 完成一个可回滚的真实地图/MOD安装。
- 使用普通账号完成一次可撤回的真实 multipart 投稿，确认服务端接收元数据、投稿包与可选 `preview` 字段；使用有权限账号确认审核、用户修改、会话撤销和审计路径。生产服务没有公开 OpenAPI，因此这些改变服务器数据的操作未擅自执行。
- 等生产 stable/beta/developer 任一通道实际发布更新包后，再执行一次生产下载与升级；本地原子替换链已验证。
- 在常用 DPI、窗口宽度和真实游戏目录下做 UI/扫描冒烟测试。
## V4.0.0-dev31 总览第二轮

- 变更前源码备份：`D:\SCFA内容中心_源码备份\V4.0.0.30_dashboard_20260822_181110`
- 总览页面新增真实最近活动和系统健康摘要；项目版本更新为 `4.0.0-dev31`。
- 待本阶段完成后执行 Release 构建、核心回归测试、窗口构造冒烟与 `artifacts\\win-x64` 发布验证。
## V4.0.0-dev32 背景网格移除

- 变更前源码备份：`D:\SCFA内容中心_源码备份\V4.0.0.31_remove-grid_20260822_184920`
- 移除主内容区 `DrawingBrush` 网格叠加，保留确认背景图片与低对比度空间装饰。
- 待本阶段完成后执行 Release 编译、核心回归测试、窗口构造冒烟和 `artifacts\\win-x64` 发布验证。
## V4.0.0-dev33 背景与卡片透明度

- 变更前源码备份：`D:\SCFA内容中心_源码备份\V4.0.0.32_panel-opacity_20260822_190817`
- 提高背景图片可见度，降低全屏遮罩与装饰强度，并为主要面板建立分级半透明背景。
- 待本阶段完成后执行 Release 编译、核心回归测试、窗口构造冒烟和 `artifacts\\win-x64` 发布验证。
