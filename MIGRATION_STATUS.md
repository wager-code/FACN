# Go dev51 → C# V4 迁移状态

| Go 模块 | C# 对应 | 状态 |
|---|---|---|
| `login_windows.go` | `Views/LoginWindow.*` + `AuthApiClient` | 已迁移主链 |
| `account_api_windows.go` / `authclient` | `AuthApiClient` + `UserSessionService` + `ManagementService` | 登录/注册/Me、DPAPI 会话、自动登录、用户角色/状态和会话撤销客户端已迁 |
| `cloud_gallery_windows.go` | `CloudPageViewModel` + `CloudCatalogService` | 正式清单、限流校验、搜索详情与手动安装已迁 |
| `local_manager_windows.go` | `LocalContentService` | 地图/MOD真实扫描、搜索详情及链接/深度/数量边界已迁 |
| `cloud_install_windows.go` | `InstallService` + `BackupService` | 下载限额、磁盘预检、安全解压、带元数据历史备份/回滚、取消与失败重试已迁 |
| `cloud_sync_windows.go` | `SyncService` | 一键同步、冲突阻断、校验错误报告与取消已迁 |
| `diagnostics_windows.go` | `LogService` + `DiagnosticsService` | 启动日志、完整诊断面板和脱敏报告导出已迁 |
| `client_update_windows.go` | `UpdateService` + `UpdateApplier` | 三通道检查、安全下载、原子自更新和清理已迁 |
| `server_manager_windows.go` | `OperationsPageViewModel` | API/投稿/Updater/直连状态和权限化运营入口已迁 |
| `submissions.go` | `SubmissionService` + `SubmissionValidator` + `SubmissionsPageViewModel` | 元数据/预览图/草稿、安全打包、双 SHA-256、真实上传进度、我的投稿与审核客户端已迁 |
| `audit_log_windows.go` | `ManagementService` + `OperationsPageViewModel` | 真实审计读取客户端已迁，受服务器 RBAC/路由约束 |
| 云端历史 | `CloudHistoryService` + `CloudHistoryPageViewModel` | COS 当前版本、真实历史接口和降级安装/本地回滚已迁 |
| `ui_*_windows.go` | WPF XAML 主题/布局 | 已脱离 Win32 手工坐标体系 |

## 迁移原则

1. 不修改服务端协议来迁就新客户端。
2. 地图版本只认 `map_version`；MOD 版本只认 `mod_info.lua` 的 `version`。
3. 云端正式清单字段继续兼容 `content_sha256` 与旧 `content_hash`。
4. 一键同步不自动降级本地更高版本。
5. ZIP 先校验、再安全解压、再备份替换，失败尽量恢复旧目录。
6. 不把 Go Win32 的窗口坐标/重绘补丁机械翻译到 WPF。


V4.0.0-dev6：统一版本来源，完成安全会话恢复/退出、首页快捷操作、设置预校验与本机构建入口修复。

V4.0.0-dev7：加固本地扫描、云端清单/下载、ZIP 解压和同步决策；增加同步取消与手动降级确认。

V4.0.0-dev8：增加云端/本地即时搜索与详情；下载任务支持可视进度、取消、失败重试和清理；设置页增加 API/COS 联合连接检测。

V4.0.0-dev9：增加首次设置向导、Steam 多库检测、目录创建和完整诊断报告。

V4.0.0-dev10：增加带元数据/SHA-256 的本地历史恢复、云端历史入口和安全客户端自更新器。

V4.0.0-dev11：增加投稿打包上传、我的投稿、审核发布、用户/会话管理、服务器状态、审计和任务历史持久化。

V4.0.0-dev12：加固自动匹配与版本比较、投稿长时上传、云端包大小校验、配置损坏恢复、登录身份复核和自更新执行校验；增加无第三方依赖的核心回归测试。

V4.0.0-dev13：修复大内容包被 20 秒全局超时中断的问题，增加 ETag/Range 断点续传和三次瞬态重试；损坏的本地地图/MOD可被精确识别并进入备份后自动修复；默认内容目录固定为玩家文档目录，游戏根目录检测要求存在可执行文件，并拒绝相对、磁盘根、重解析点及互相嵌套的内容目录。

V4.0.0-dev14：重构云端地图/MOD内容页，增加全文搜索、安装状态/分类/收藏/最近安装筛选、名称/发布时间/大小/状态排序、扩展详情和持久化收藏；支持勾选筛选结果后安全批量安装/更新/修复、逐项失败隔离与取消，批量流程不会自动降级本地较新或无法比较的版本。成功安装会自动进入最近内容列表。

V4.0.0-dev15：完善玩家投稿主链：支持选择或拖入内容目录、填写并恢复本机草稿、校验名称/版本/作者/说明/分类/标签、选择并验证真实 PNG/JPEG 预览图；投稿包继续执行安全打包与双 SHA-256，上传显示真实进度并可取消/重试，“我的投稿”改用生产服务实际存在的 `/v1/submissions/mine` 路由。

V4.0.0-dev16：重构软件设置为事务式编辑：输入先进入独立配置快照，未保存修改不再影响当前服务；支持放弃修改、保留账号/收藏的推荐默认值、目录只读预检和保存时创建。账号 API、COS 正式清单、投稿/更新健康状态可使用尚未保存的参数分项或整体只读检测；配置原子落盘后统一切换运行快照，API/TLS 变化时安全清除旧会话并要求重新登录。

V4.0.0-dev17：完成 UI 视觉重构第一阶段：建立统一深色科幻设计系统，重做按钮、输入框、下拉框、复选框、表格、标签页、进度条、滚动条和卡片；主窗口使用自绘框架、Fluent 图标导航、选中态、页面上下文顶栏及紧凑侧栏。登录页和首页重新设计并使用真实状态数据，实际截图复核 1040×660 登录窗口、1586×992 主窗口和 980×680 紧凑布局。

V4.0.0-dev17.1：修复投稿页只读内容版本使用默认双向绑定而触发的 WPF 运行时异常，并同步加固设置页的只读路径绑定；增加静态只读绑定检查和独立投稿页面真实 WPF 加载测试。

V4.0.0-dev18：完成内容管理 UI 第二阶段：云端地图/MOD增加目录、安装和待处理统计，统一搜索筛选、状态徽标、批量操作和详情面板；本地地图/MOD增加有效/异常统计、结构状态与安全卸载详情；同步中心重构为同步范围、安全原则、进度状态和执行记录控制台；下载任务增加队列统计、状态徽标、进度与空状态。四页均使用真实数据绑定并完成 1500×900 与 980×680 实际窗口检查。

V4.0.0-dev19：完成投稿与维护工作区 UI 第三阶段：投稿中心增加四步工作流、草稿完成度、字段计数、预览与投稿记录详情；本地备份和云端历史增加真实分类/容量/版本统计及所选项目安全操作区；诊断中心改为系统健康控制台；客户端更新改为发布通道、安全校验、版本详情与暂存状态控制台。五页继续使用真实服务数据，不生成演示内容。

V4.0.0-dev20：完成管理员工作区 UI 第四阶段：投稿审核增加真实队列类型/容量统计、投稿元数据/双哈希详情与审核决策台；用户管理增加正常/受限账号及活动会话统计、搜索目录和角色/状态/会话受控操作台；服务器审计增加四项服务健康、审计全文搜索、异常统计和所选记录详情。全局表格选中态同步修正为深色主题，980×680 紧凑操作区支持滚动访问；所有写操作继续保留原 RBAC 校验和二次确认。

V4.0.0-dev21：完成首次设置与软件设置 UI 第五阶段：首次设置改为发现游戏、准备独立玩家目录、验证保存三步工作区，实时显示检测数量、目录健康和完成度；软件设置按游戏与目录、网络与内容、客户端行为、服务器路径、本机数据五类分组，增加草稿状态、配置完整度、目录/API/COS/服务器健康摘要和固定保存操作区。所有检测仍只读，目录仅在最终保存时创建，候选配置仍在全量校验与原子落盘成功后生效。

V4.0.0-dev21.1：修复非标准安装无法保存游戏目录的问题。游戏识别同时检查根目录和 `bin`：标准主程序名称保持兼容；改名主程序必须具有有效 PE 头，并与 `game.dat` 及至少两项 SCFA 特征文件同目录。常见 `game.exe` 优先于崩溃报告器等辅助程序，其他未知名称只从不少于 1MB 的 PE 中按体积筛选；本机 `E:\SCFA` 已准确定位至 `E:\SCFA\bin\game.exe`。同步仍只操作独立玩家 Maps/Mods 目录，游戏根目录可不配置。

V4.0.0-dev22：落实用户确认的目录、同步与 UI 方案。地图/MOD 根目录必须由用户选择且已真实存在，软件不再回退到 C 盘“我的文档”或自动创建猜测路径；同步页改为主窗口内唯一实例，跨页面继续运行并保留进度/记录，方案 A 导航显示同步百分比、活动下载数与诊断提醒。严格一致性将目录全部文件纳入指纹，同版本多余文件也会触发备份后整目录替换；清单缺少内容指纹时采用严格重新安装，避免不同玩家留下不同残余文件。云端历史增加实时搜索、清除与计数，云端地图/MOD详情增加正式或显式本地预览、无图占位、版本/大小/作者/分类/标签和实际安装路径。主界面加入克制的行星、战略网格和等高线背景；诊断中心增加改名 EXE、LAA、DirectX 9 与 VC++ x86 组件检测，并保持运行补丁安装锁定，直至具备可信清单、SHA-256/签名、确认和可回滚备份。窗口关闭流程改为用户关闭时无条件显式退出，只有登录页与主界面内部交接才抑制退出，修复无窗口后台残留进程。

V4.0.0-dev22.1：修复 dev22 登录成功后主窗口无法创建的问题。战略网格的 `GeometryDrawing.Pen` 改为显式 WPF `Pen` 对象；账号与密码等通用输入框内容宿主按 `VerticalContentAlignment` 居中，解决文字贴底和裁切。增加由主程序真实入口执行的隐藏主窗口构造测试，发布前同时验证 apphost 和最终单文件 EXE，避免仅做 XML/编译检查遗漏运行时 XAML 类型转换问题。

V4.0.0-dev22.2：完成登录页人性化修正。账号和密码输入模板改为由边框承载内边距并让内容宿主垂直居中，真实窗口渲染验证文字和密码圆点不再裁切；增加记住账号、记住密码选项，密码仅在登录成功后使用 Windows DPAPI 当前用户范围加密保存，取消选项会清除相应缓存；账号、注册邮箱、密码、记住选项和按钮建立明确 Tab 顺序，账号/邮箱按回车进入下一输入项，登录页按回车直接提交。

V4.0.0-dev23：修复全量审计发现的核心一致性问题。总览的一键同步改为复用同步中心的唯一任务实例，切换页面继续显示进度、结果并可取消；同步服务拒绝第二个并发同步，安装与卸载统一串行化，避免同时替换同一内容目录。地图扫描同时兼容 `map_version` 与标准旧地图使用的 `version`，消除原版/旧版地图被错误标记异常的问题。所有展示型 DataGrid 默认只读，云端批量选择使用独立复选框；用户与审计窄窗口列表改为紧凑双行信息结构并启用单元格裁剪，避免文字重叠和误读。

V4.0.0-dev24：完善面向普通用户的登录体验。登录页默认勾选“记住账号”，账号输入框支持下拉切换多个已保存账号；“记住密码”和“自动登录”保持关闭，自动登录只在用户明确勾选后执行。旧版单账号配置会自动迁移到最多 8 个账号记录，密码继续使用 Windows 当前用户加密保存。登录页将 COS、TLS、证书指纹等开发术语改为客户可理解的同步、安全连接和隐私说明，并重新排列 Tab 顺序与按钮文案。

V4.0.0-dev25：完成客户视角界面收尾第一批。普通账号隐藏连接服务和服务器路径高级设置；设置页顶部指标、诊断页标题和操作按钮改用客户语言；设置完成度按当前用户可见字段计算；主界面战略网格、轨道线和等高线背景提高可见度并降低遮罩压暗。Release 编译 0 警告、0 错误，核心回归测试全部通过。

V4.0.0-dev26：同步中心增加本地同步历史持久化。同步完成、取消或失败后保存最近结果，重新打开软件可恢复最近一次执行记录；历史最多保留 30 次，采用临时文件替换避免写入中断损坏。同步中心增加本次记录与历史记录计数。

V4.0.0-dev27：背景视觉第二轮优化。主内容区从单一网格与右上环形结构扩展为低对比度斜向光带、连续等高线、分段航迹和多层环形结构，降低遮罩覆盖并保持内容面板可读性；未引入第三方图片资源。

V4.0.0-dev28：接入用户确认的 2048×1152 指挥舰桥背景图片。主内容区使用自适应铺满与深色可读性遮罩，原有战术纹理降为低透明度叠层；新增背景资源 `Assets/command_deck_background.png`。

V4.0.0-dev29：按用户确认替换主内容区背景图片为蓝色行星与机甲场景；图片尺寸 `1586×992`，使用 `UniformToFill` 铺满，图片透明度调整为 0.52，可读性遮罩调整为 0x82，保留原有低透明度战术纹理。

V4.0.0-dev30：修复云端历史页面只读统计属性绑定方向。`ContentCount`、`FilteredContentCount`、`VersionCount` 明确使用 `Mode=OneWay`，避免 WPF 运行时将只读属性当成 TwoWay 或 OneWayToSource 写入目标。
## V4.0.0-dev31

- 总览第二轮：补齐最近活动与系统健康区域，数据来自本地任务、内容目录、磁盘、网络配置和客户端运行环境。
- 总览底部信息改为可滚动布局，避免 980×680 紧凑窗口中内容被裁切；沿用用户确认的蓝色行星与机甲背景。
## V4.0.0-dev32

- 按用户反馈移除主内容区额外网格叠加层，保留确认的蓝色行星与机甲背景、暗色遮罩、光带、圆环和低对比度路径线。
- 背景回归检查改为确认“不包含 DrawingBrush 网格”且保留图片和空间结构，版本更新为 `4.0.0-dev32`。
## V4.0.0-dev33

- 根据实际截图提高背景图片可见度：图片不透明度由 0.52 调整为 0.72，全屏深色遮罩由 0x82 降至 0x4D，装饰层同步减弱。
- 通用卡片、重点卡片、工具栏、内嵌区和流程卡采用分级半透明深色背景；表格行继续保持清晰对比。

## V4.0.0-dev34

- 补齐个人资料窗口与侧栏头像入口；显示名称、QQ、手机号和头像仅保存在本机。
- 修正安装与投稿服务构造函数变化造成的回归测试编译错误。
- Windows CI 在发布前运行核心回归测试和个人资料窗口加载检查。
- 本地源码备份统一保存在项目内 `_源码备份`，该目录由 `.gitignore` 排除。

## V4.0.0-dev35

- 地图/MOD 玩家主链增加隔离目录端到端验证：安装、同步、内容差异修复、安全卸载与备份恢复。
- 自动更新只允许覆盖所选 Maps/Mods 根目录的直接子目录，避免覆盖包装目录内的嵌套内容。
- 一键同步阻止云端清单重复目标文件夹或多个项目重复匹配同一个玩家目录。
- 真实游戏目录验收仍需用户提供当前使用的 Maps/Mods 路径；隔离测试不写入游戏目录。

## V4.0.0-dev36

- 使用本机已保存的管理员账号核对生产服务：身份、我的投稿、用户和审计只读接口正常；审核列表默认路径返回 404，当前不能宣称审核链路已完成服务端验收。
- 审核页面采用服务端实际返回的 `review.read`、`review.approve` 权限；只读审核员可以读取队列但不能提交决定。
- 用户、审计和内容下架入口统一按精确角色及权限判断，避免角色名包含 `admin` 字样时误显管理员入口。
- `E:\SCFA\maps`、`E:\SCFA\mods` 已完成只读扫描与云端内容指纹核对，未修改玩家文件。
